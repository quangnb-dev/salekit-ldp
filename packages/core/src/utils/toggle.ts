export const toggleBounding = (visible: boolean): boolean => !visible;
export const toggleGuideline = (visible: boolean): boolean => !visible;
export const toggleResizeHandle = (visible: boolean): boolean => !visible;
