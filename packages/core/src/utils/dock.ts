export const genDockVal = (value: string): string => value;

export const toggleDockVal = (visible: boolean): boolean => !visible;

export const genDockLine = (value: string): string => value;

export const toggleDockLine = (visible: boolean): boolean => !visible;
